def square_of_numbers():
    x = 10
    y = int(input("Enter a number you want to calculate square of ? ")) 
    print(f"The numbers are {x, y}")

    x_square = x * x
    y_square = y * y
    print(f"Square of number {x} = {x_square}\nSquare of number {y} = {y_square}") 
square_of_numbers()
